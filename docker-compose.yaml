services:
  # Production/stable build compose file.
  # Browser-facing domains, CORS, auth URLs, and cookie/JWT secrets come from
  # the shared app env files. Dev overrides must not replace them.

  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: medusa_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: medusa-store
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - medusa_network

  # Redis
  redis:
    image: redis:7-alpine
    container_name: medusa_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    networks:
      - medusa_network

  # Medusa backend and admin dashboard. Production runs the built image.
  medusa:
    build:
      context: .
      dockerfile: Dockerfile
      secrets:
        - backend_env
    container_name: medusa_backend
    restart: unless-stopped
    depends_on:
      - postgres
      - redis
    working_dir: /server/apps/backend
    ports:
      - "9000:9000"
    environment:
      NODE_ENV: production
      PORT: 9000
    env_file:
      - apps/backend/.env
    networks:
      - medusa_network

  # Next.js storefront. Production runs the built image.
  storefront:
    build:
      context: .
      dockerfile: apps/storefront/Dockerfile
      secrets:
        - storefront_env
    container_name: medusa_storefront
    restart: unless-stopped
    depends_on:
      - medusa
    working_dir: /server/apps/storefront
    ports:
      - "8000:8000"
    environment:
      NODE_ENV: production
      PORT: 8000
    env_file:
      - apps/storefront/.env
    networks:
      - medusa_network

volumes:
  postgres_data:

networks:
  medusa_network:
    driver: bridge

secrets:
  backend_env:
    file: ./apps/backend/.env
  storefront_env:
    file: ./apps/storefront/.env
